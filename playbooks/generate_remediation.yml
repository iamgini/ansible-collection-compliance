---
- name: Generate targeted Ansible remediation playbook from scan results
  hosts: "{{ target_hosts | default('all') }}"
  gather_facts: true
  become: false
  collections:
    - community.scap_hardening

  roles:
    - role: community.scap_hardening.generate_fix
