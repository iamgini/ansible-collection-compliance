---
- name: Generate targeted Ansible remediation playbook from scan results
  hosts: "{{ target_hosts | default('scan_targets') }}"
  gather_facts: true
  become: false
  collections:
    - iamgini.compliance

  roles:
    - role: iamgini.compliance.oscap_generate_fix
