---
# Inventory for SCAP Compliance Scanning
# Organize hosts by compliance profile group

all:
  children:
    # RHEL9 hosts - CIS Level 2 Server Profile
    rhel9_cis_l2:
      hosts:
        rhel9-server-01:
          ansible_host: # Add IP or hostname here
          ansible_user: # Add SSH user here (e.g., ansible, root, ec2-user)
          # ansible_ssh_private_key_file: ~/.ssh/id_rsa  # Uncomment if using SSH key
          # ansible_become_pass: !vault |...  # Uncomment if sudo password needed

      vars:
        # Profile configuration (already set in group_vars/linux_cis_l2.yml)
        # Override here only if different from group defaults

    # Additional groups for other profiles (uncomment as needed)
    # rhel9_cis_l1:
    #   hosts:
    #     rhel9-web-01:
    #       ansible_host:
    #       ansible_user:
    #   vars:
    #     # Uses group_vars/linux_cis_l1.yml

    # rhel9_stig:
    #   hosts:
    #     rhel9-secure-01:
    #       ansible_host:
    #       ansible_user:
    #   vars:
    #     # Uses group_vars/linux_stig.yml
